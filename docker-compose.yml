version: "3.9"

services:

  app:
    build: .

    ports:
      - "3000:3000"

    env_file:
      - .env

  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: nnodes

    ports:
      - "5432:5432"